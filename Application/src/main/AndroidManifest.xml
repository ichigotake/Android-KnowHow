<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.ichigotake.sandbox.app" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!-- TODO: MainActivity に各種アクティビティへのリンクを設置して、これを **.category.LAUNCHER にする -->
        <activity
            android:name="net.ichigotake.sandbox.app.MainActivity"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name="net.ichigotake.sandbox.app.fragment.collaboration.basic.SampleActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
